import "dotenv/config";import {z} from "zod";
const s=z.object({NODE_ENV:z.enum(["development","test","production"]).default("development"),PORT:z.coerce.number().int().min(1).max(65535).default(8000),NAVAMSHA_BASE_URL:z.string().url().default("https://api.navamsha.in"),NAVAMSHA_API_KEY:z.string().min(1),NAVAMSHA_AYANAMSHA:z.string().default("lahiri"),NAVAMSHA_OBSERVATION_POINT:z.enum(["geocentric","topocentric"]).default("geocentric"),BHAVISHYA_INTERNAL_API_KEY:z.string().min(16),NOMINATIM_BASE_URL:z.string().url().default("https://nominatim.openstreetmap.org"),GEOCODER_USER_AGENT:z.string().min(5),GEOCODER_TIMEOUT_MS:z.coerce.number().int().min(1000).max(30000).default(10000),CORS_ORIGIN:z.string().default("*"),NAVAMSHA_TIMEOUT_MS:z.coerce.number().int().min(1000).max(60000).default(15000),NAVAMSHA_MAX_RETRIES:z.coerce.number().int().min(0).max(5).default(2)});export const env=s.parse(process.env);