import type {Request,Response,NextFunction} from "express";import {env} from "../config/env.js";export function internalApiKey(req:Request,res:Response,next:NextFunction){if(req.header("X-Bhavishya-API-Key")!==env.BHAVISHYA_INTERNAL_API_KEY){res.status(401).json({error:{code:"UNAUTHORIZED",message:"Invalid or missing API key."}});return}next()}